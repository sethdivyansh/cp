#include<bits/stdc++.h>
using namespace std;
#define pll pair<long long,long long>
#define N 100000

vector<pll> m;

int n;

pll ma(int i){
	if(m[i].first) return m[i];
	if(i==n-1) return make_pair
}

int main(){
	
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cin>>n;
	int ar[n];
	for(int i=0;i<n;i++){
		cin>>ar[i];
	}

}